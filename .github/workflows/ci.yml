name: CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - run: docker build -t analyzer .

      - run: |
          docker run --rm \
          -e GITHUB_USER="${{ github.actor }}" \
          analyzer
